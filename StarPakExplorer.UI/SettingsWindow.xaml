<Window x:Class="StarPakExplorer.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Width="760"
        Height="520"
        MinWidth="700"
        MinHeight="380"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Padding="14"
                Background="#F4F7FB"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="asset_unpacker.exe"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold" />
                <TextBox Grid.Column="1"
                         Text="{Binding AssetUnpackerPath, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,8,8" />
                <Button Grid.Column="2"
                        Content="Browse..."
                        Command="{Binding BrowseUnpackerCommand}" />

                <TextBlock Grid.Row="1"
                           Text="Default PAK folder"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold" />
                <TextBox Grid.Row="1"
                         Grid.Column="1"
                         Text="{Binding PakParentDirectory, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,8,8" />
                <Button Grid.Row="1"
                        Grid.Column="2"
                        Content="Browse..."
                        Command="{Binding BrowsePakDirectoryCommand}" />

                <TextBlock Grid.Row="2"
                           Text="asset_packer.exe"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold" />
                <TextBox Grid.Row="2"
                         Grid.Column="1"
                         Text="{Binding AssetPackerPath, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,8,8" />
                <Button Grid.Row="2"
                        Grid.Column="2"
                        Content="Browse..."
                        Command="{Binding BrowsePackerCommand}" />

                <TextBlock Grid.Row="3"
                           Text="Patch folder"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold" />
                <TextBox Grid.Row="3"
                         Grid.Column="1"
                         Text="{Binding PatchRootDirectory, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,8,8" />
                <Button Grid.Row="3"
                        Grid.Column="2"
                        Content="Browse..."
                        Command="{Binding BrowsePatchRootCommand}" />

                <TextBlock Grid.Row="4"
                           Text="Cache folder"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold" />
                <TextBox Grid.Row="4"
                         Grid.Column="1"
                         Text="{Binding CacheRootDirectory, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,8,8" />
                <Button Grid.Row="4"
                        Grid.Column="2"
                        Content="Browse..."
                        Command="{Binding BrowseCacheRootCommand}" />

                <TextBlock Grid.Row="5"
                           Text="Translation folder"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold" />
                <TextBox Grid.Row="5"
                         Grid.Column="1"
                         Text="{Binding TranslationRootDirectory, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,8,8" />
                <Button Grid.Row="5"
                        Grid.Column="2"
                        Content="Browse..."
                        Command="{Binding BrowseTranslationRootCommand}" />

                <TextBlock Grid.Row="6"
                           Text="全局术语表:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <TextBox Grid.Row="6"
                         Grid.Column="1"
                         Text="{Binding GlobalGlossaryPath, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center"/>
                <Button Grid.Row="6"
                        Grid.Column="2"
                        Content="浏览..."
                        Command="{Binding BrowseGlobalGlossaryCommand}"
                        Padding="8,3"/>

                <Button Grid.Row="7"
                        Grid.ColumnSpan="3"
                        Content="从术语库导入..."
                        Command="{Binding ImportTermBankCommand}"
                        HorizontalAlignment="Left"
                        Padding="8,3"
                        Margin="0,4,0,0"/>

                <Button Grid.Row="8"
                        Grid.ColumnSpan="3"
                        Content="导出术语库..."
                        Command="{Binding ExportTermBankCommand}"
                        HorizontalAlignment="Left"
                        Padding="8,3"
                        Margin="0,4,0,0"/>

                <TextBlock Grid.Row="9"
                           Grid.ColumnSpan="3"
                           Text="Leave blank to use the default LocalAppData folder."
                           Foreground="#667"
                           Margin="0,8,0,0" />
            </Grid>
        </Border>

        <DockPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       VerticalAlignment="Center"
                       Foreground="#43546A"
                       DockPanel.Dock="Left" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="88" Command="{Binding CancelCommand}" />
                <Button Content="Save" Width="88" Command="{Binding SaveCommand}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
